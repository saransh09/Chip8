mod cpu;
mod display_manager;
mod frame_buffer;
mod keypad;
mod memory;

use cpu::*;
use display_manager::*;
use frame_buffer::*;
use keypad::*;
use memory::*;

fn main() {
    println!("Hello, world!");
}
